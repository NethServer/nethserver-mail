# altermime disclaimer

dfilt     unix    -       n       n       -       -       pipe
      flags=Rq user=mail null_sender=
      argv=/usr/libexec/nethserver/disclaimer-send $\{sender\} $\{recipient\}
