#
# 60smtpd_restrictions -- see 00template_vars fragment for default array settings..
#

smtpd_client_restrictions = { join(",\n ", @smtpd_client_restrictions); }

smtpd_helo_restrictions = { join(",\n ", @smtpd_helo_restrictions); }

smtpd_sender_restrictions = { join(",\n ", @smtpd_sender_restrictions); }

smtpd_recipient_restrictions = { join(",\n ", @smtpd_recipient_restrictions); }

smtpd_relay_restrictions = { join(",\n ", @smtpd_relay_restrictions); }


