#
# Reject SMTP sender by top level domain name suffix
#
{
    return '# - disabled' if( ! $rspamd{'SenderBlackList'});
    $OUT = '';
    foreach my $item ( split(',', lc($rspamd{'SenderBlackList'})) ) {
        if ($item !~ m/@/) {
            $OUT .= "$item\n";
        }
    }
}
