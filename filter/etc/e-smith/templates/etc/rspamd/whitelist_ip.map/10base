
#
# whitelist IP in rspamd
#

{
    if(defined $postfix{AccessBypassList}) {
        foreach my $ip (split(',', ($postfix{AccessBypassList}))) {
        push @whitelist, $ip;
        }

        foreach my $lines (@whitelist){
        $OUT .= "$lines\n";
        }
    }
}
