
# Enable/disable greylisting
{
my $grey = (($rspamd{SpamGreyLevelStatus} || 'disabled') eq 'disabled') ? 'false':'true';
$OUT .= "enabled = $grey;"
}

# Redis server to store greylist
servers = /var/run/redis-rspamd/rspamd;

